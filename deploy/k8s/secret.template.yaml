# Template — NÃO comite valores reais. Gere com:
#   kubectl -n tupan create secret generic tupan-web-secrets \
#     --from-literal=REGISTRY_PULL_TOKEN=... --dry-run=client -o yaml
apiVersion: v1
kind: Secret
metadata:
  name: tupan-web-secrets
  namespace: tupan
type: Opaque
stringData:
  REGISTRY_PULL_TOKEN: "troque-me"
